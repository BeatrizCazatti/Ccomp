/*Escreva um algoritmo que leia dois números inteiros representando duas cidades e imprima o menor caminho de uma para outra. O grafo que lista as ligações e distâncias entre cada cidade é fornecido a partir do arquivo <Mapa.txt>, que contém 3 colunas de valores inteiros: cidade1, cidade2 e distância. As colunas são separadas por espaço e cada linha representa uma ligação entre cidades.

O programa deve imprimir duas linhas que demonstram o caminho mais curto a ser percorrido e a distância total conforme exemplo abaixo:

Menor caminho entre a cidade 8 e a cidade 5: 8 2 5

Distancia total: 6

A imagem abaixo fornece uma representação visual das cidades (vértices) e as estradas que as conectam (arestas) com a distância entre elas.


[image0.png]*/